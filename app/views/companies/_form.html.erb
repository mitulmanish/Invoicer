


<%= form_for(@company) do |f| %>
	<div>
		<%= f.label :name %>
		<%= f.text_field :name %>
	</div>
	<div>
		<%=  @company.new_record? ? "Create Company" : "Update Company"f.submit  %>
	</div>
<% end %>